# Test dependencies for the Wattpilot integration test suite.
#
#   pip install -r requirements_test.txt
#   pytest
#
# The YAML-catalog and const tests need only pytest + pyyaml. The utils and
# config-flow tests additionally import the integration, which pulls in Home
# Assistant and the wattpilot-api client library; without those they skip.

pytest>=7.0
pytest-cov>=5.0
pyyaml>=5.3.0

# Runtime deps required to import the integration package (see manifest.json).
# Home Assistant itself is heavy; pull it via the community test helper which
# also provides HA pytest fixtures for future full-integration tests.
pytest-homeassistant-custom-component
wattpilot-api>=1.4.0
aiofiles>=23.2.1
packaging>=24.0

# Type stubs for the untyped runtime deps. mypy --strict (a Platinum
# quality-scale rule) fails with import-untyped without them, so they are
# declared here rather than left to whatever a developer happens to have
# installed -- which is exactly how CI and local runs drifted apart.
types-aiofiles>=23.2
types-PyYAML>=6.0
